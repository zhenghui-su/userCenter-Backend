spring:
  application:
    name: user-center
    # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zhenghui
    username: root
    password: 695469208lqt.
  # session 失效时间
  session:
    timeout: 86400
